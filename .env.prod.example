## Production environment example: copy to .env.prod and replace placeholders with real credentials.

# =======================================
# SMARTACCOUNTING — PRODUCTION ENVIRONMENT
# =======================================

# ───────────────
# Server Settings
# ───────────────
NODE_ENV=production
PORT=5000
HOST=0.0.0.0
TRUST_PROXY=false

# ───────────────
# Frontend / Domain
# ───────────────
CLIENT_URL=https://ai-accounting.app
FRONTEND_URL=https://ai-accounting.app
CORS_ORIGIN=https://ai-accounting.app

# ───────────────
# Database (SQLite – Production)
# ───────────────
DATABASE_URL=sqlite:///usr/src/app/database/smartaccounting.db

# ───────────────
# Authentication
# ───────────────
# Replace with a securely generated 32+ character secret
JWT_SECRET=replace_with_a_secure_32_char_secret
JWT_REFRESH_SECRET=replace_with_a_secure_refresh_secret
JWT_REFRESH_EXPIRES_IN=7d
JWT_EXPIRES_IN=24h

# ───────────────
# Email (Production – placeholder)
# ───────────────
EMAIL_HOST=smtp.yourprovider.com
EMAIL_PORT=587
EMAIL_USER=no-reply@ai-accounting.app
EMAIL_PASS=replace_with_secure_email_password
EMAIL_FROM="SmartAccounting <no-reply@ai-accounting.app>"
EMAIL_ENABLED=false

# ───────────────
# Stripe (Production – optional)
# ───────────────
STRIPE_SECRET_KEY=replace_with_stripe_secret_key
STRIPE_WEBHOOK_SECRET=replace_with_stripe_webhook_secret
STRIPE_PUBLISHABLE_KEY=replace_with_stripe_publishable_key

# ───────────────
# File Uploads
# ───────────────
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10000000

# ───────────────
# Security Settings
# ───────────────
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX=200

# ───────────────
# API
# ───────────────
API_BASE_URL=/api
# Cookies must remain secure in HTTPS production.
SECURE_COOKIES=true

# ───────────────
# Logging
# ───────────────
LOG_LEVEL=info
LOG_FILE=/usr/src/app/logs/app.log
REQUEST_LOGGING=true
METRICS_ENABLED=false

# ───────────────
# German Tax / ELSTER
# ───────────────
ELSTER_ENDPOINT=https://www.elster.de
ELSTER_TEST_MODE=false

# ───────────────
# OCR Engine (optional)
# ───────────────
OCR_SERVICE_URL=
OCR_API_KEY=

# ───────────────
# Cache (disabled)
# ───────────────
REDIS_URL=
CACHE_TTL=3600
