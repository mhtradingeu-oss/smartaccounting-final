# Example environment variables for SmartAccounting Frontend
# API base URL for the backend (use `/api` when proxying through nginx)
VITE_API_URL=/api
# App version (auto-injected)
VITE_APP_VERSION=0.1.0
# Feature flags (comma-separated, e.g. ai,vat,gdpr)
VITE_FEATURE_FLAGS=
