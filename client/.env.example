# Example environment variables for SmartAccounting Frontend
# Copy to .env and adjust as needed


# === FINAL APPROVED API STRATEGY ===
#
# DEV (Local, no Docker):
# VITE_API_URL=   # Leave empty to use Vite proxy (/api)
#
# DEV (Docker Compose):
# VITE_API_URL=http://localhost:5001/api
#
# PROD (Reverse Proxy):
# VITE_API_URL=   # Leave empty, frontend calls /api
#
# ❌ Never use Docker service names (e.g., http://backend:5000/api) in browser env!

VITE_API_URL=
VITE_APP_ENV=development
VITE_AI_ASSISTANT_ENABLED=true
