# Example environment variables for SmartAccounting Frontend
# Copy to .env and adjust as needed

# Docker Compose development assumes the backend is reachable at the service alias
VITE_API_URL=http://backend:5000/api
# If you run the frontend outside Compose (e.g., Vite dev server on your laptop),
# override VITE_API_URL to target localhost:5000 or another backend host.
VITE_APP_ENV=development
# Add any additional frontend env vars here
VITE_AI_ASSISTANT_ENABLED=true
